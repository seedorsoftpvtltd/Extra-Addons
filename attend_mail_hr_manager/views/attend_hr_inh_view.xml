<odoo>
<data>

    <record id="attendance_mail_hr_manager_form" model="ir.ui.view">
        <field name="name">attendance.mail.hr.manager.inhs</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <!-- Locate the position to add a new tab "My New Tab" in "Form View Sale Order"-->
            <xpath expr="//field[@name='parent_id']" position="after">
                <field name="att_mail"/>
                <field name="att_mail1"/>
                <field name="hr_manager"/>
           </xpath>
        </field>

    </record>
    <!-- This is to add our field in the quotation saler order and print -->
</data>
</odoo>
