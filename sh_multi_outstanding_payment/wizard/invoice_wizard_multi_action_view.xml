<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_invoice_outstanding_payment_wizard" model="ir.ui.view">
        <field name="name">view.invoice.outstanding.payment.wizard</field>
        <field name="model">sh.invoice.wizard</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <group>
                <group>
                    <field name="partner_id" />
                    <field name="date"/>
                    <field name="account_id" />
                </group>
                <group></group>
            </group>

            <group string="Outstanding Payments">
                <field name="sh_move_line_ids" nolabel="1">
                    <tree>
                        <field name="date"/>
                        <field name="name"/>
                        <field name="move_id"/>
                        <field name="move_line_id" invisible="1"/>
                        <field name="amount"/>
                        <field name="sh_boolean" widget="boolean_toggle"/>
                    </tree>
                </field>
            </group>

            <group string="Invoice Details" >
                <field name="sh_invoice_ids"  nolabel="1">
                    <tree>
                        <field name="name" />
                        <field name="invoice_origin" />
                        <field name="company_id" />
                        <field name="invoice_date_due" />
                        <field name="amount_untaxed_signed" />
                        <field name="amount_total_signed" />
                        <field name="amount_residual_signed" />
                        <field name="state" />
                    </tree>
                </field>
            </group>
            <footer>
                <button  string="Outstanding Payment"  name="add_outstanding_payment_wizard" type="object" class="oe_highlight" icon="fa-money" />
                <button string="Cancel" class="btn btn-secondary" special="cancel" icon="fa-remove" />
            </footer>
        </field>
    </record>

    <record id="action_view_invoice_outstanding_payment_wizard_view" model="ir.actions.act_window">
        <field name="name">Outstanding Payment</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sh.invoice.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
