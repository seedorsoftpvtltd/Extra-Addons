<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_view_task" model="ir.actions.act_window">
        <field name="name">Employee Tasks</field>
        <field name="res_model">project.task</field>
        <field name="view_mode">kanban,tree,form,calendar,pivot,graph,activity</field>
        <field name="context">{'search_default_my_tasks': 1}</field>
        <field name="search_view_id" ref="project.view_task_search_form"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new task
            </p><p>
                Odoo's project management allows you to manage the pipeline of your tasks efficiently.
                <br/>
                You can track progress, discuss on tasks, attach documents, etc.
            </p>
        </field>
    </record>
    <menuitem action="action_view_task" id="menu_taskcreate" parent="menu_selfservice" sequence="3" />
</odoo>